import React from 'react';
import ReactDOM from 'react-dom/client';
import Feed from '../src/pages/feed/feed';

ReactDOM.createRoot(document.getElementById('root-feed')!).render(
  <React.StrictMode>
    <Feed />
  </React.StrictMode>,
);
